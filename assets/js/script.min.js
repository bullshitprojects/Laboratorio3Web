$(document).ready(function() {
    ! function() {
        if ("requestAnimationFrame" in window && !/Mobile|Android/.test(navigator.userAgent)) {
            var n = [];
            if ($("[data-bs-parallax-bg]").each(function() {
                    var t = $(this),
                        e = $("<div>");
                    e.css({ backgroundImage: t.css("background-image"), backgroundSize: "cover", backgroundPosition: "center", position: "absolute", height: "200%", width: "100%", top: 0, left: 0, zIndex: -100 }), e.appendTo(t), n.push(e[0]), t.css({ position: "relative", background: "transparent", overflow: "hidden" })
                }), n.length) {
                var t, e = [];
                $(window).on("scroll resize", o), o()
            }
        }

        function o() {
            e.length = 0;
            for (var o = 0; o < n.length; o++) {
                var a = n[o].parentNode.getBoundingClientRect();
                a.bottom > 0 && a.top < window.innerHeight && e.push({ rect: a, node: n[o] })
            }
            cancelAnimationFrame(t), e.length && (t = requestAnimationFrame(i))
        }

        function i() {
            for (var n = 0; n < e.length; n++) {
                var t = e[n].rect,
                    o = e[n].node,
                    i = Math.max(t.bottom, 0) / (window.innerHeight + t.height);
                o.style.transform = "translate3d(0, " + -50 * i + "%, 0)"
            }
        }
    }()
}), $(function() { new Swiper(".swiper-container", { loop: !0, pagination: ".swiper-pagination", paginationClickable: !0, nextButton: ".swiper-button-next", prevButton: ".swiper-button-prev" }) });